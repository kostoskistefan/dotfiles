[module/time-date]
type = internal/date
interval = 1

date = %H:%M %a %d %b
label = %date%
label-foreground = ${colors.foreground}

format-prefix = %{T2}  %{T-}
format-prefix-foreground = ${colors.primary}

click-left = bash -c '$HOME/.config/polybar/scripts/calendar/calendar.sh'
